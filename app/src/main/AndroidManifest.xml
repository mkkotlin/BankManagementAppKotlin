<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mayank.loterabank">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LoteraBank">
        <activity
            android:name=".Record"
            android:exported="true" />
        <activity android:name=".OneRecord" />
        <activity android:name=".DeleteAccount" />
        <activity android:name=".Deposit" />
        <activity android:name=".Withdraw" />
        <activity android:name=".OpenAccount1" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>